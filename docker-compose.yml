services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: base-project-arvrcv
    restart: unless-stopped
    ports:
      - "${HOST_PORT:-5000}:5000"
    environment:
      PORT: 5000
      DEBUG: "false"
      APP_ACCESS_PASSWORD: ${APP_ACCESS_PASSWORD:-}
      FLASK_SECRET_KEY: ${FLASK_SECRET_KEY:-}
      SESSION_COOKIE_SECURE: ${SESSION_COOKIE_SECURE:-false}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
    volumes:
      - ./models:/app/models
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:5000/healthz"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s
